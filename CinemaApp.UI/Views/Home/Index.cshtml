@model IEnumerable<CinemaApp.BL.DTOs.MovieDTOs.Movie.MovieDTO>
@{
    ViewData["Title"] = "Головна сторінка";
}

@if (Model == null || !Model.Any())
{
    <p class="text-center mt-4">Немає доступних фільмів.</p>
}
else
{
    <div class="container">
        <div class="row justify-content-center">
            @foreach (var movie in Model)
            {
                <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                    <div class="card text-center shadow-sm">
                        <img src="@movie.PosterURL" class="card-img-top" alt="Постер фільму"
                             style="height: 250px; width: 100%; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title">@movie.Title</h5>
                            <p class="card-text"><strong>Жанр:</strong> @movie.GenreName</p>
                            <p class="card-text"><strong>Рейтинг:</strong> @movie.Rating</p>
                            <a asp-controller="Movie" asp-action="Details" asp-route-id="@movie.MovieID" class="btn btn-primary btn-sm">Деталі</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}
